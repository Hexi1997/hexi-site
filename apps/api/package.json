{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply hexi-site --remote"
  },
  "dependencies": {
    "@workspace/api-rpc": "workspace:*",
    "better-auth": "^1.5.5",
    "drizzle-orm": "^0.45.1",
    "drizzle-zod": "^0.8.1",
    "hono": "^4.12.8",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.9",
    "wrangler": "^4.4.0"
  }
}
